<div class="list-group">
  <%= link_to project.name ,admin_project_path(project), class: "list-group-item list-group-item-action list-group-item-success"%>
  <%= link_to [:admin,project], remote: true, method: :delete,  data: { confirm: 'Are you sure?' } do %>
  <button class="btn btn-primary">Delete!</button> 
  <% end %>
</div>